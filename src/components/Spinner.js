   // src/components/Spinner.js

   import React from 'react';
   import './Spinner.css'; // Ensure you have a corresponding CSS file for styling

   const Spinner = () => {
       return (
           <div className="spinner"></div>
       );
   };

   export default Spinner;